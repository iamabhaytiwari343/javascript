// Create a new string
var myString = "hello world";

// Get the length of the string
var length = myString.length;

// Convert the string to uppercase
var uppercaseString = myString.toUpperCase();

// Find the index of a substring
var index = myString.indexOf("world");

// Replace a substring with another string
var newString = myString.replace("world", "everyone");

var myString = "Hello, world!";
var length = myString.length; // 13

var uppercaseString = myString.toUpperCase(); // "HELLO, WORLD!"

var lowercaseString = myString.toLowerCase(); // "hello, world!"

var character = myString.charAt(1); // "e"

var subset = myString.substring(1, 4); // "ell"

var words = myString.split(" "); // ["Hello,", "world!"]

var index = myString.indexOf("world"); // 7

