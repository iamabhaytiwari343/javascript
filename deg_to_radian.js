//degree to radian
function degToRad(degrees) {
    return degrees * (Math.PI / 180);
  }
  // radian to degree
  function radToDeg(rad) {
    return rad / (Math.PI / 180);
  }

  console.log(degToRad(165))
  console.log(radToDeg(2))

  